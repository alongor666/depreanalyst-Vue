# 📋 项目独立化迁移检查清单

## ✅ 现状评估

### 代码依赖检查
- [x] **无外部引用**：所有 `../` 路径都是项目内部引用
- [x] **独立依赖管理**：拥有 `package.json` 和 `node_modules`
- [x] **完整配置**：`.eslintrc.cjs`, `.prettierrc`, `vite.config.js` 等
- [x] **独立文档**：项目级 `CLAUDE.md` 已完整

---

## 📦 可选迁移资源

### 父目录内容价值评估

| 资源类型 | 路径 | 价值评级 | 是否需要迁移 | 说明 |
|---------|------|---------|------------|------|
| **📖 项目文档** | `docs/CLAUDE.md` | ⭐⭐ | ❌ 不需要 | 描述的是旧版静态项目，与 Vue 版本架构不同 |
| **📖 结构文档** | `docs/PROJECT_STRUCTURE.md` | ⭐ | ❌ 不需要 | 旧版目录结构，不适用于 Vue 项目 |
| **📖 评价报告** | `docs/评价报告.md` | ⭐⭐⭐ | ⚠️ 可选 | 包含项目评价和改进建议，可作参考 |
| **📖 快速参考** | `docs/QUICK_REFERENCE.md` | ⭐⭐ | ⚠️ 可选 | 使用场景参考，可能有启发 |
| **📚 HTML 指南** | `guides/*.html` | ⭐ | ❌ 不需要 | 旧版静态页面，功能已在 Vue 版本实现 |
| **⚙️ 开发脚本** | `scripts/*.py`, `scripts/*.sh` | ⭐ | ❌ 不需要 | 仅适用于静态 HTML 项目 |
| **🎯 技能包** | `skill-package/*.skill` | ⭐⭐⭐⭐ | ✅ 建议 | Claude Code 技能包，可能包含有用的提示词 |

---

## 🚀 推荐迁移步骤

### 步骤 1：创建参考文档目录

```bash
cd /Users/xuechenglong/Downloads/深度研究skills/deep-reading-analyst
mkdir -p docs/references
```

### 步骤 2：复制有价值的参考文档（可选）

```bash
# 复制评价报告（包含改进建议）
cp ../docs/评价报告.md docs/references/

# 复制快速参考（使用场景）
cp ../docs/QUICK_REFERENCE.md docs/references/

# 复制技能包（Claude Code 技能）
mkdir -p skill-package
cp -r ../skill-package/* skill-package/
```

### 步骤 3：更新 .gitignore（确保不忽略新文件）

检查 `.gitignore` 是否会排除新添加的文件：

```bash
# 检查 gitignore 规则
cat .gitignore
```

### 步骤 4：提交更改

```bash
git add .
git commit -m "docs: 添加父项目参考文档

- 复制评价报告作为改进参考
- 复制快速参考文档
- 添加 Claude Code 技能包
- 准备独立化迁移"
```

---

## 🎯 方案二：完全隔离（下阶段迁移）

### 迁移目标
将 `deep-reading-analyst/` 移至完全独立的目录，切断与父目录的所有关系。

### 执行步骤

```bash
# 1. 在 Downloads 创建新目录
mkdir ~/Downloads/deep-reading-analyst-standalone

# 2. 复制项目（保留 git 历史）
cp -r /Users/xuechenglong/Downloads/深度研究skills/deep-reading-analyst/ ~/Downloads/deep-reading-analyst-standalone/

# 3. 验证独立性
cd ~/Downloads/deep-reading-analyst-standalone
npm install
npm run dev
npm run build

# 4. 确认一切正常后，更新远程仓库（如有必要）
git remote -v
# 如果需要更新仓库地址：
# git remote set-url origin <新仓库地址>
```

### 迁移后清理（可选）

```bash
# 删除父目录中的 deep-reading-analyst/ 文件夹
cd /Users/xuechenglong/Downloads/深度研究skills/
rm -rf deep-reading-analyst/

# 或者重命名为备份
mv deep-reading-analyst/ deep-reading-analyst-backup/
```

---

## ✅ 验证清单

### 迁移前验证
- [ ] 项目可以正常启动：`npm run dev`
- [ ] 项目可以正常构建：`npm run build`
- [ ] 所有测试通过：`npm test`
- [ ] Git 历史完整：`git log`
- [ ] 没有未提交的更改：`git status`

### 迁移后验证
- [ ] 新位置可以正常启动
- [ ] 所有页面功能正常
- [ ] 路由跳转正常
- [ ] 数据加载正常
- [ ] 生产构建正常
- [ ] 无控制台错误

---

## 🔗 相关资源

### 项目内部文档
- `CLAUDE.md` - Vue 项目开发指南
- `README.md` - 项目说明
- `package.json` - 依赖管理

### Git 相关
```bash
# 查看项目历史
git log --oneline --graph --decorate --all

# 检查远程仓库
git remote -v

# 查看分支
git branch -a
```

---

## 📝 注意事项

### ⚠️ 重要提醒
1. **不要删除父目录**，直到完全确认新项目独立运行正常
2. **保留 Git 历史**：使用 `cp -r` 而非移动，确保历史记录完整
3. **测试完整性**：迁移后运行完整的测试套件
4. **更新文档**：更新 README.md 中的路径引用（如有）

### 💡 最佳实践
- 先在新位置测试 1-2 周，确保稳定后再清理旧目录
- 保留父目录作为参考，直到确认不再需要
- 定期提交 Git，方便回滚

---

**文档版本**: v1.0
**创建日期**: 2025-12-29
**适用范围**: deep-reading-analyst Vue 项目独立化迁移
